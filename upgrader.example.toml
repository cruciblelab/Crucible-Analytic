# Config for the schema upgrader (cmd/upgrader). Copy to upgrader.toml,
# or point -config at it directly.
#
# This is the sixth binary and the only one allowed to change the shape of
# the database. The panel writes a row saying it wants an upgrade; this
# reads that row, applies the schema and writes back what happened. See
# cmd/upgrader/main.go for why it is separate at all.
#
# # This file is not like the other five
#
# It carries the only DSN in the deployment that can run DDL, so it is the
# only configuration file the services' own account must not be able to
# read. install.sh gives it to a second account - crucible-upgrader - at
# mode 0640, and the panel's account is not in that group. If you install
# by hand, do the same; a `chmod 644` here hands the panel's process the
# ability to rewrite its own database.

# Required. Must name schema_admin, the fifth role, which owns the tables.
#
# Not one of the four service roles, and the difference is the whole
# design: release/sql/grants.sql contains no ALTER, no CREATE and no OWNER
# for collector, beacon_writer, analytics_reader or panel_user. An
# upgrader pointed at panel_user would connect happily and then fail on
# the first ALTER, half way through a migration.
#
# install.sh creates the role and writes its password in here.
schema_admin_dsn = "postgres://schema_admin:change-me@localhost:5432/analytics"

# How often to look for a waiting request, in seconds. Default 30.
#
# Only used when the process runs as a daemon - the Docker entry point, or
# `upgrader` with no flags. Under systemd it is the timer that decides,
# and this is ignored: the unit runs with -once and exits. Changing the
# systemd cadence means editing crucible-upgrader.timer, not this line.
#
# Thirty seconds because somebody just pressed a button and is watching a
# page. The query is one indexed lookup that finds nothing almost every
# time.
# interval_seconds = 30

# Logging. The defaults send everything to stderr, which is what a
# systemd timer unit wants: `journalctl -u crucible-upgrader` is where an
# operator looks, and a one-shot process that writes its own log file
# leaves an operator two places to look instead of one.
#
# The lines a *customer* needs do not depend on this. They go to the panel
# through the log sink, which is a database table rather than a file - an
# upgrade is the one operation somebody watches a page for.
#
# Set dir to write files as well; the four services default to
# /var/log/crucible-analytic.
[logging]
# dir = ""
# level = "info"

# ---------------------------------------------------------------------
# Panelden güncelleme: paketler nereden gelir, ve neyle doğrulanır
# ---------------------------------------------------------------------
#
# Bu bölüm **yoksa panelde güncelleme düğmesi de yoktur.** Güvenli yön
# budur: paketlerinin nereden geleceği söylenmemiş bir kurulum tahmin
# etmemeli.
#
# İki değer de burada, ve **veritabanında değil.** Sebep bu bölümün
# tamamı:
#
#   Bu dosya mod 0640 ve crucible-upgrader grubunun. Dört servis
#   `crucible` olarak koşuyor ve bu dosyayı okuyamıyor. Yani ele
#   geçirilmiş bir panel "şu sürüme geç" diye bir satır yazabilir, ama
#   paketin nereden geleceğini de neyle doğrulanacağını da
#   değiştiremez.
#
# Adres veritabanında olsaydı panel indirmeyi yönlendirebilirdi.
# Anahtar veritabanında olsaydı panel kendi paketini imzalatabilirdi.
# İkisi de burada olduğu için, düğme kod çalıştırma yolu değil.
#
# [release]
#
# Paketlerin bulunduğu dizin. Yükseltici sonuna sürümü ve dosya adını
# ekler; istek satırından buraya sürümden başka hiçbir şey geçmez, ve
# sürümün şekli de ayrıca kontrol edilir.
#
# **https zorunlu.** Düz http'de yoldaki herkes, gerçekten bizim
# imzaladığımız **eski** bir sürümü servis edebilir — ve imza bunu
# reddetmez, çünkü o sürümü biz de imzaladık. Bilinen açığı olan bir
# sürüme geri düşürmek, imzanın durdurmadığı bir saldırı.
# base_url = "https://surumler.example.com/crucible-analytic"
#
# Paketin SHA256SUMS dosyasını doğrulayan açık anahtar. Sürüm
# sayfasında yayınlanır; gizli değil, ama **doğru olması** gerekiyor.
# İkisinden biri yazılıp diğeri yazılmazsa yükseltici açılmaz:
# imzasız bir güncelleme yolu, biraz daha zayıf bir güncelleme yolu
# değil, ağdan kod çalıştırma özelliğidir.
# prefix — kurulum kökü. Binary'ler prefix/bin altındadır.
# Varsayılan /opt/crucible-analytic, yani install.sh'ın kullandığı yer.
# Başka bir yere kurduysanız burayı yazın: yanlış dizine kurulan bir
# güncelleme sessizce başarılı olur, servisler eskisini çalıştırmaya
# devam eder ve sayfa "kuruldu" der.
# prefix = "/opt/crucible-analytic"

# public_key = "..."

# [backup]
#
# Yedek dosyalarının yazılacağı dizin. Boş bırakılırsa bu kurulum yedek
# almaz: paneldeki düğme bir istek satırı yazar, yükseltici onu alır ve
# satıra "yedek dizini yapılandırılmamış" yazıp bitirir. Sessizce
# beklemez.
#
# Yol **buraya** yazılır, istek satırına değil. İstek satırında bir yol
# olsaydı, ele geçirilmiş bir panelin seçebileceği bir yol olurdu ve
# yükseltici her tablonun dökümünü oraya yazardı — root'un dizini, başka
# bir müşterinin ağacı, bir web kökü. Satır neyin dahil edileceğini
# söyler; nereye gideceğini söylemez.
#
# **systemd altında /var/lib/crucible-analytic altında olmalı.** Öneri
# değil, gereklilik: birim dosyası ProtectSystem=strict taşıyor, yani
# dosya sisteminin tamamı bu servis için salt-okunur, ve yazılabilir tek
# yol ReadWritePaths ile o dizin. systemd upgrader.toml'u okuyamaz, o
# yüzden yazılabilir yol orada sabit, dizin ise burada seçiliyor; ikisini
# uzlaştıran şey bu.
#
# Başka bir yere işaret ederseniz yükseltici açılışta günlüğe hata
# yazıyor ("dizin yazılamıyor"). Sessizce beklemiyor, çünkü bu ayarın
# yanlış olması yalnız yedeği değil şema yükseltmesini de durduruyor:
# yedeği yapılandırmış bir kurulumda kopya alınamazsa yükseltme
# başlamıyor.
#
# **Konteynerde volume üzerinde olmalı.** Durum dizininin altı bu yüzden
# de öneriliyor: compose dosyası orayı volume olarak bağlıyor, ve volume
# dışına yazılan bir yedek bir sonraki imaj güncellemesinde silinir — ki
# o, yedeğin karşı sigorta olduğu kaybın ta kendisidir.
#
# Dizin 0700, dosyalar 0600 olarak oluşturulur ve yükselticinin
# hesabına aittir. Dört servisin çalıştığı `crucible` hesabı onları
# okuyamaz: bir yedek, o servislerin okumasına izin verilmeyen her
# satırı ve panelin parola hash'lerini taşır.
# dir = "/var/lib/crucible-analytic/yedek"
#
# Sırlar yedeğinin kapatılacağı açık anahtar.
#
# Boş bırakılırsa bu kurulum sırlar yedeği almaz: panelde kutu görünür,
# istek satırı yazılır, ve yükseltici satıra "alıcı yapılandırılmamış"
# yazıp bitirir. Veri yedekleri bundan etkilenmez.
#
# Üretmek için, geliştirici parolasıyla:
#
#     devpass -recipient
#
# Aynı parolayı kullanın: `[developer] password_hash` ile bu satır aynı
# parolanın iki ayrı türevidir ve birbirlerinden üretilemezler.
#
# **Bu satır açık anahtardır.** Sırlar yedeğini kapatmaya yarar, açmaya
# yaramaz. Açan tek şey parolanın kendisidir ve o parola sunucuda hiçbir
# yerde yazılı değildir — ne bu dosyada, ne diskte, hiçbir yerde. Yani
# bu makine bir sırlar yedeği üretebilir ve üretttiğini okuyamaz; root
# da okuyamaz. Yedek dizini kopyalanmış, disk imajı alınmış, sanal
# makine hurdaya çıkmış olsun: parola olmadan hepsi işe yaramaz.
#
# Bunun bedeli açıkça yazılmalı: parolayı kaybederseniz o güne kadar
# alınmış bütün sırlar yedekleri açılamaz hâle gelir. Tasarım budur.
# Yeni bir parola üretip bu satırı değiştirmek yalnız bundan sonrakileri
# kurtarır.
#
# Sırlar yedeğinin içinde bu dizindeki yapılandırma dosyaları vardır:
# beş rolün veritabanı parolaları, panelin oturum anahtarı ve
# `ip_hash_key`. Veri yedeğiyle aynı dosyaya asla girmezler — ikisi bir
# aradaysa takma adlandırma çözülür.
# recipient = "cadev1.131072.3.4.<tuz>.<açık anahtar>"
#
# Yedeğin gerçekten geri döndüğünü sınamak için kullanılan **yan
# veritabanı**.
#
# Boş bırakılırsa bu kurulum geri yükleme provası yapamaz: paneldeki
# düğme "yan veritabanı yapılandırılmamış" der. Veri yedeği almak ve
# doğrulamak bundan etkilenmez.
#
# **Bu veritabanının içindeki her şey her denemede silinir.** Adı burada
# durur, istek satırında değil — ele geçirilmiş bir panelin
# seçebileceği bir ad olsaydı, seçtiği veritabanı silinirdi.
#
# Veritabanını bir kez siz oluşturursunuz; yükseltici oluşturamaz ve
# oluşturamaması kasıtlıdır (schema_admin'de CREATEDB yok):
#
#     CREATE DATABASE analitik_dogrulama OWNER schema_admin;
#
# Canlı veritabanına işaret ederse reddedilir, ve reddeden şey adların
# karşılaştırılması değil: yükseltici canlı bağlantıda bir advisory lock
# tutup hedefte aynı anahtarı deniyor. PostgreSQL bu kilidi
# veritabanına göre kapsıyor, yani deneme yalnız ikisi aynı
# veritabanıysa başarısız oluyor. Yazımlar farklı olabilir, cevap
# değişmez.
#
# İkinci koruma asıl uygulayan: içinde tablo olan ve bu özelliğin
# bıraktığı `panel_restore_target` işaretini taşımayan bir veritabanı
# reddedilir. Yani yanlışlıkla başka bir gerçek veritabanına işaret
# etmek de silinmesine yol açmaz.
#
# Canlıya geçiş burada yok ve olmayacak — o adım kabukta kalıyor.
# restore_dsn = "postgres://schema_admin:PAROLA@127.0.0.1/analitik_dogrulama"
#
# Bir yedeğin ne kadar süre saklanacağı, gün olarak.
#
# **Yazılmazsa yedekler süresiz saklanır**, ve bugünkü davranış budur.
# Varsayılanın silme olmamasının sebebi şu: bir sürüme geçen müşterinin
# yedekleri o gün silinseydi, istemedikleri bir şeyi istedikleri başka bir
# şeyin yan etkisi olarak yapmış olurduk.
#
# Ama sınırsız olması sessiz kalmasın diye değil. Kapatmak istediği tuzak
# şu: **saklama politikası analitik satırları yaşına göre siler, ondan
# önce alınmış bir yedek o satırları hâlâ taşır.** Yani yedek dizini,
# saklama süresinin sessizce geçerli olmadığı tek yer — ve müşteriye de
# ziyaretçilerine de verilen söz o sayıdır.
#
# Uygulayan yükselticidir: önce dosyayı siler, sonra katalog satırını
# unutur. Ters sıra, kimsenin bilmediği bir dosya bırakırdı — hiç
# doğrulanmayacak, hiç listelenmeyecek, hiç silinmeyecek bir yedek.
#
# **En yeni veri yedeği hiçbir zaman silinmez**, sınırdan eski olsa bile.
# Dört yüz gün önce alınmış tek yedeği olan bir kurulum, üç yüz günlük bir
# sınır yüzünden hiç yedeksiz kalırdı; "eski bir yedek" ile "yedek yok"
# aynı ölçeğin iki noktası değil. Bu bir ayar değil, güvence: öbür
# davranışı isteyen bir kurulum yok, ve ayar olsaydı yanlışlıkla
# istenebilirdi.
#
# **Sırlar yedeklerine dokunmaz.** İçlerinde ziyaretçi verisi yok —
# yapılandırma dizininin kapatılmış hâli — yani yukarıdaki cümle onlara
# ulaşmıyor. Ve bir makineyi geri getiren dosya odur: son sırlar yedeği
# süpürülmüş bir kurulumun geri yükleyebileceği bir veritabanı vardır ve
# ip_hash_key'i yoktur, ki o olmadan saklanmış her takma ad anlamsızdır.
# Onları budamak farklı şekilde bir karar ve operatörde kalıyor.
#
# Tavan 730 gün, saklama politikasının tavanıyla aynı. Negatif sayı
# reddedilir: "sınırsız" demenin yolu 0'dır, ve -30 yazıp otuz gün
# kastetmiş birine sınırsız vermek yalnız bir kez nazik olurdu.
# keep_days = 90
